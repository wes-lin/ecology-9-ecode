# 2026-07-02

## vscode-ecode multi-environment support

- Began adding multi-environment support for the VS Code extension.
- Added `ecode.environments` configuration for named environments with:
  - environment name
  - eCode server URL
  - username
  - password
  - local working directory
- Added `ecode.activeEnvironment` and the `ecode.switchEnvironment` command in the `ecodeExplorer` view title area.
- Environment switching uses a VS Code QuickPick and stores the selected active environment in global user settings, not project/workspace settings.
- Removed the legacy flat config compatibility path per preference; the extension now expects environment-based configuration.
- Extracted environment-related types and helpers into `src/config/ecodeEnvironment.ts` instead of putting all logic into `treeDataProvider.ts`.
- Cookie storage is now derived per environment/account using environment name, normalized base URL, and username, so sessions are isolated across environments and accounts.
- Updated provider state handling so switching/refreshing clears the SDK client, tree cache, virtual file content cache, and snapshot store.
- Adjusted local storage layout:
  - downloaded source files go under `localDir/src`
  - metadata goes under `localDir/.ecode/`
  - app config path is `localDir/.ecode/ecode-apps.json`
  - snapshot path is `localDir/.ecode/snapshots.json`
- Started moving app config and snapshot path resolution away from the workspace `.ecode` directory and toward the active environment root.

## vscode-ecode app commands and context menu grouping

- Added `debugMode?: 'y' | 'n'` to remote tree item/node metadata and app config output.
- Mapped remote `debugMode` into `EcodeNode` and show a debug icon when `debugMode === 'y'`.
- Added command contributions and registrations for:
  - `ecode.createNewApp`
  - `ecode.createNewType`
  - `ecode.createNewFolder`
  - `ecode.createNewJs`
  - `ecode.createNewCss`
  - `ecode.createNewMd`
  - `ecode.renameItem`
  - `ecode.uploadResource`
- Added context values for new command visibility:
  - type folders expose `canCreateNewApp` and `canCreateNewType`
  - app folders expose `canCreateNewFolder`, `canCreateNewJs`, `canCreateNewCss`, and `canCreateNewMd`
  - deletable nodes expose `canRename`
  - resource folders expose `canUploadResource`
- Simplified command titles:
  - `Set Preload` -> `Preload`
  - `Set Preload Order` -> `Preload Order`
  - `Create New ...` -> `New ...`
- Grouped the explorer context menu:
  - `navigation@0`: refresh
  - `inline@0-1`: view/compare
  - `new@0-6`: new app/type/folder/files and upload resource
  - `release@0-1`: release/cancel release
  - `preload@0-2`: preload/cancel preload/preload order
  - `modify@0-1`: rename/delete
- Added placeholder handlers for new/rename commands until the remote APIs are wired.
- Added `uploadResource()` using `vscode.window.showOpenDialog()` to select one local file and report the selected path.

## Notes

- The implementation has not yet been fully built/verified because the build command was interrupted before running.
- Next step is to run `pnpm --filter vscode-ecode build`, fix any TypeScript/package issues, and manually verify switching plus download paths in the extension host.
- New app/type/folder/file, rename, and upload resource are currently command/UI entry points only; remote API integration still needs to be implemented.
