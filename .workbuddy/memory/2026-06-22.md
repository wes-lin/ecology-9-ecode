# 2026-06-22

- 调试 `vscode-ecode` 插件在 VS Code 中不可见问题：
  - 更新 `.vscode/launch.json` 提供两种启动配置：`Run Extension (monorepo root)` 和 `Run Extension (package folder)`，适配不同打开方式。
  - 在 `extension.js` 新增 `ecode.openSettings` 命令，并在 `package.json` 注册，同时在 `eCode Explorer` 视图标题栏添加设置按钮。
  - 帮助排查：必须按 `F5` 启动 Extension Host，且需要在 Extension Host 中打开文件夹才能看到 `eCode Explorer` 视图。
- 将 `eCode Explorer` 从 Explorer 侧栏内部视图改为独立的 Activity Bar 容器，添加 `media/ecode.svg` 图标，现在在左侧活动栏会有 eCode 图标。
- 将 eCode 活动栏面板拆分为上下两部分：
  - 上部分 `Settings`：使用 Webview View 显示配置表单，可一次性修改 Server、Account、Password、Local Dir。
  - 下部分 `Explorer`：仅显示 ecode 目录树。
  - 新增 `src/settingsWebviewProvider.js`，删除 `src/settingsProvider.js`。
  - 保存后自动刷新 Explorer 视图。
- 修复 Webview View 注册错误：在 `package.json` 的 `views` 中为 `ecodeSettings` 添加 `"type": "webview"`，否则 VS Code 会将其当作普通树视图并提示没有 data provider。
- 为 Settings Webview 表单中的参数添加图标：Server 🌐、Account 👤、Password 🔒、Local Dir 📁，保存按钮添加 💾。
- 为工程完善 ESLint 自动格式化与 jsconfig.json：
  - 根目录添加 `eslint.config.js`（ESLint 9 flat config），使用 `@eslint/js` + `eslint-config-prettier`。
  - 添加 `.prettierrc`（单引号、120 列宽、2 空格缩进）。
  - 添加 `.vscode/settings.json`，配置保存时自动 Prettier 格式化 + ESLint 自动修复。
  - 添加 `jsconfig.json`，为 `ecode-sdk` 提供路径别名提示，支持 VS Code 的 JavaScript 智能提示。
  - 修复 `packages/vscode-ecode/package.json` 中 Activity Bar 图标路径错误（`assets` → `media`）。
  - 为 `vscode-ecode` 补充空的 `test` 脚本，使 `npm test --workspaces` 可正常工作。
  - 运行 `npm run format`、`npm test`、`npm run lint` 全部通过。
- 将 `ecode-sdk` 改为基于 Cookie 的会话管理：
  - 新增 `CookieJar` 类，自动解析 `Set-Cookie` 并在后续请求中携带 `Cookie` 头。
  - `login()` 后从响应头中提取 Cookie 存入 Jar，后续请求不再显式传 session。
  - 新增 `CookieJar` 单元测试，运行 `npm test` 通过。
- 实现 Cookie 持久化：
  - `CookieJar` 支持 `loadFromFile` / `saveToFile`，将 Cookie 以 JSON 格式持久化到本地文件。
  - `EcodeClient` 构造函数接收 `cookieFile` 选项，初始化时自动加载已有 Cookie，登录成功后自动保存。
  - 新增 `isLoggedIn()` 和 `logout()` 方法。
  - VS Code 插件中通过 `context.globalStorageUri` 指定 Cookie 文件路径（`cookies.json`），实现登录一次、下次自动复用。
  - `treeDataProvider` 在刷新/下载前检查 `isLoggedIn()`，仅在没有有效 Cookie 时才调用登录接口。
  - 运行 `npm run format`、`npm run lint`、`npm test` 全部通过。
